import { Bug, FileText, Radar, ShieldCheck, type LucideIcon } from "lucide-react";

export const AGENT_ICONS: Record<string, LucideIcon> = {
  radar: Radar,
  bug: Bug,
  "shield-check": ShieldCheck,
  "file-text": FileText,
};
